export const API_URL = import.meta.env.VITE_BACKEND_URL || 'https://jobgenesis-backend.onrender.com'; // Use your likely Render URL or fallback
// Note: In production, ensure VITE_BACKEND_URL is set in your Vercel project settings.
